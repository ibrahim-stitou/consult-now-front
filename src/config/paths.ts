const ROOTS ={
  ADMIN:'/admin',
}

// paths for the admin section
export const PATHS = {
  ROOTS,
  admin: {
    trackers: {
      list:{
        link: `${ROOTS.ADMIN}/trackers`,
      },
    },
  },
};